#As tuplas são consideradas listas em Python, mas os dados são imutáveis(são fixos)
    #tupla = (1, 2, 3)

tupla = (1, 2, 3)

print(tupla)
print(type(tupla))

#tupla[0] = 10 / irá dar erro pois o tipo tupla não suporta alterações

print(tupla[1]) #porem podemos usar normalmente